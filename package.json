// package.json (na raiz)
{
  "name": "gitnations-engine",
  "version": "1.0.0",
  "description": "Motor de simulação para GitNations",
  "main": "js/game.js",
  "scripts": {
    "start-turn": "node js/game.js" 
  },
  "dependencies": {
    // Adicione dependências futuras aqui (ex: octokit, probot)
  }
}
